import { Metadata } from 'next';
import { Suspense } from 'react';

export const metadata: Metadata = {
    title: 'home',
};

const Page = () => {
    return <div>Home</div>;
};

export default Page;
